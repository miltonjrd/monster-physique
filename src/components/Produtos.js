import styled from 'styled-components';

const Products = () => {
    return (
        <section className="container">
            <h3>Produtos</h3>
        </section>
    );
}

export default Products;